---
description: Drop items into the GTD Inbox (one per line).
---

Invoke the `gtd` skill to run `capture` with arguments: $ARGUMENTS

See skills/gtd/SKILL.md §1 for behavior. If no arguments, ask the user what to capture.

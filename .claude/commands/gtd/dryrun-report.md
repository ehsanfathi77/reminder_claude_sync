---
description: Report what the engine would do without writing.
---

Invoke the `gtd` skill to run `dryrun-report` with arguments: $ARGUMENTS

Flags: `--days N`, `--json`. See skills/gtd/SKILL.md §12.

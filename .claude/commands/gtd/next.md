---
description: Show next actions filtered by context, time, energy.
---

Invoke the `gtd` skill to run `next` with arguments: $ARGUMENTS

Flags: `--ctx X`, `--time Nm`, `--energy low|med|high`. See skills/gtd/SKILL.md §3.
